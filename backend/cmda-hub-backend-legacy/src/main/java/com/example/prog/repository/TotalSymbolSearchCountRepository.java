package com.example.prog.repository;

import com.example.prog.entity.TotalSymbolSearchCount;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TotalSymbolSearchCountRepository extends JpaRepository<TotalSymbolSearchCount, String> {
}