name: Build & Push Backend Docker Image

on:
  push:
    branches: [main]
    paths:
      - "backend/cmda-hub-backend-legacy/**"
      - ".github/workflows/backend-docker-build.yml"
  pull_request:
    branches: [main]
    paths:
      - "backend/cmda-hub-backend-legacy/**"
      - ".github/workflows/backend-docker-build.yml"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Set up Docker Buildx for efficient multi-stage builds
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.GITHUBACTIONSCCICD }}

      # 4. Extract short SHA for tagging
      - name: Extract Git SHA
        id: vars
        run: echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      # 5. Build and push the Docker image
      - name: Build and Push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend/cmda-hub-backend-legacy
          file: ./backend/cmda-hub-backend-legacy/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/cmda-hub-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/cmda-hub-backend:${{ steps.vars.outputs.SHORT_SHA }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
